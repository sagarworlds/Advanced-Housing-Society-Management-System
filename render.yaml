services:
  - type: web
    name: society-management-api
    runtime: dotnet
    plan: free
    rootDir: backend
    buildCommand: dotnet publish SocietyManagement.Api/SocietyManagement.Api.csproj -c Release -o out
    startCommand: dotnet out/SocietyManagement.Api.dll
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: Jwt__Key
        generateValue: true
      - key: Jwt__Issuer
        value: SocietyManagementApi
      - key: Jwt__Audience
        value: SocietyManagementApiUsers

  # Frontend Static Site (Angular PWA)
  services:
  - type: web       # <--- Always use 'web' for static sites
    runtime: static # <--- This defines it as a static site
    name: society-management-ui
    plan: free
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist/society-management-app/browser
    envVars:
      - key: NODE_VERSION
        value: 22.12.0
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
